<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reset Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 flex items-center justify-center h-screen text-white">

    <div class="w-full max-w-md bg-zinc-800 p-8 rounded-xl shadow-lg">
        
        <h2 class="text-2xl font-semibold text-center mb-6">
            Change Password
        </h2>

        <form action="/resetpassword" method="POST" class="space-y-4">

            <!-- Old Password -->
            <div>
                <label class="block mb-1 text-sm">Old Password</label>
                <input 
                    type="password" 
                    name="oldPassword"
                    placeholder="Enter old password"
                    class="w-full px-4 py-2 rounded-md bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                >
            </div>

            <!-- New Password -->
            <div>
                <label class="block mb-1 text-sm">New Password</label>
                <input 
                    type="password" 
                    name="newPassword"
                    placeholder="Enter new password"
                    class="w-full px-4 py-2 rounded-md bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                >
            </div>

            <!-- Confirm New Password -->
            <div>
                <label class="block mb-1 text-sm">Confirm New Password</label>
                <input 
                    type="password" 
                    name="confirmPassword"
                    placeholder="Confirm new password"
                    class="w-full px-4 py-2 rounded-md bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required
                >
            </div>

            <!-- Submit Button -->
            <button 
                type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 transition py-2 rounded-md font-medium"
            >
                Update Password
            </button>

        </form>

        <!-- Optional Link -->
        <p class="text-center text-sm text-gray-400 mt-4">
            Remembered your password?
            <a href="/login" class="text-blue-400 hover:underline">Login</a>
        </p>

    </div>

</body>
</html>
